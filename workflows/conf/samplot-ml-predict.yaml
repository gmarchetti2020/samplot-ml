samples:
  # TODO script to generate this config:
  #    * Especially if there are going to be a lot of samples
  # eg SAMPLE: "PATH/TO/BAM_OR_CRAM"
  # or SAMPLE: "s3://BUCKET/BAM_OR_CRAM"
  # or SAMPLE: "http://URL/BAM_OR_CRAM"
  HG03687: "data/HG03687.final.cram"

samples_idx:
  # TODO script to generate this config:
  #    * Especially if there are going to be a lot of samples
  # eg SAMPLE: "PATH/TO/BAI_OR_CRAI"
  # or SAMPLE: "s3://BUCKET/BAI_OR_CRAI"
  # or SAMPLE: "http://URL/BAI_OR_CRAI"
  HG03687: "data/HG03687.final.cram.crai"

fasta:
  data_source: "local" # local or s3
  file: "data/GRCh38_full_analysis_set_plus_decoy_hla.fa"
fai:
  data_source: "local" # local or s3
  file: "data/GRCh38_full_analysis_set_plus_decoy_hla.fa.fai"

vcf:
  data_source: "local" # local of s3
  file: "data/HG03687-smoove.genotyped.vcf.gz"

image_filename_delimiter: "-"
outdir: "output"
